# This bundle's one runtime dependency is a leaf utility package, and the
# private mirror configured for the harness profile lags the versions the
# harness itself ships (its `@deepseek-ai/cosmokit` pin is missing there), so
# this package resolves from public npm. Scoped to this directory only.
registry=https://registry.npmjs.org/